# Sample App — Overview (placeholder)

STUB CONTENT. Real sample insurance/financial application content is out of
scope for this scaffolding phase — this fixture exists only to prove the
CodeDesignContext interface boundary end-to-end.

The sample application is a policy quoting service. It exposes a REST API
for creating and pricing insurance quotes, backed by a rules engine and a
customer profile store.
