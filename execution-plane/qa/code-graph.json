{
  "_comment": "Seeded code-intelligence graph. In production this is derived from the repository and generated as a pinned export of the context graph \u2014 it is hand-authored here only until that export exists. Paths are repository-relative, matching what `git diff --name-status` emits: they used to be relative to demo-app/ and matched only because the lookup compared by substring, which also matched paths that had nothing to do with the module.",
  "depends_on": [
    {
      "from": "claims-table",
      "to": "claims-api"
    },
    {
      "from": "claims-filter",
      "to": "claims-api"
    }
  ],
  "modules": [
    {
      "id": "claims-table",
      "paths": [
        "demo-app/app/claims/page.tsx"
      ],
      "covered_by": [
        "claims-table-renders"
      ]
    },
    {
      "id": "claims-filter",
      "paths": [
        "demo-app/app/claims/page.tsx"
      ],
      "covered_by": [
        "filter-approved",
        "filter-denied",
        "filter-empty-state"
      ]
    },
    {
      "id": "claims-api",
      "paths": [
        "demo-app/app/api/claims/route.ts"
      ],
      "covered_by": [
        "api-status-param"
      ]
    }
  ]
}
